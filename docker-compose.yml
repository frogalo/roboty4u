services:
  roboty4u:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3011:3011"
    env_file:
      - .env.local
    restart: unless-stopped
    volumes:
      # Mount videos at runtime so you can add new ones without rebuilding
      - ./public/videos:/app/public/videos:ro
